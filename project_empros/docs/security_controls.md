### AI Security & Adversarial Defenses (OWASP & MITRE ATLAS)
---

Sentinel Nexus natively enforces the OWASP Top 10 for LLMs and the MITRE ATLAS frameworks at the code level, ensuring the cognitive swarm remains deterministic and tamper-resistant.

The primary purpose is to track implementation, and enhance/refine during the future roadmap iteration.

The following manifest is currently the base controls implemented pertaining to AI security.  Furthermore, system/network security controls have been implemented during the infrastructure design phase (ansible/terraform/init) within the IaC directory `infrastructure/`.

> For how this manifest maps against the NIST AI 600-1 (Generative AI Profile) risks and GOVERN/MAP/MEASURE/MANAGE actions — including coverage ratings and prioritized gaps — see [nist_ai_600_1_control_tracker.md](nist_ai_600_1_control_tracker.md).

---

**Manifest:**

* Canary Token Verification (OWASP LLM01 / ATLAS AML.T0042): The orchestrator injects dynamically generated UUID canary tokens into the system prompts. If an adversary successfully executes a prompt injection payload that forces the model to leak its instructions, the orchestrator detects the canary in the output and instantly halts the SOAR pipeline.
* Strict Output Enforcement (OWASP LLM07 & LLM08): To prevent hallucinated capabilities or excessive agency (e.g., executing unauthorized APIs), all Swarm execution plans are validated against a rigid `SoarExecutionSchema`. Any deviations drop the payload before it hits the NATS bus.
* Model Denial of Service (OWASP LLM04): LangGraph execution loops are bounded by strict `recursion_limit` constraints and absolute `asyncio` timeouts, neutralizing attacks designed to saturate GPU VRAM via infinite context generation.
* Sybil RLHF Poisoning Quarantine (OWASP LLM03 / ATLAS AML.T0031): The `worker_rlhf` microservice monitors both individual and global operator override velocity. Coordinated attempts to poison the Model D (Critic) reward pipeline via malicious manual dismissals trip an atomic circuit breaker, quarantining the tainted data.
* Data Loss Prevention (OWASP LLM06): The `CognitiveSanitizer` middleware scrubs RFC 1918 internal IP ranges and high-entropy credentials from payloads before they can be routed to Frontier Models (Anthropic/Azure), guaranteeing sovereign data isolation.


* **Cognitive Boundary Isolation (OWASP LLM01 / ATLAS AML.T0043):** The `CognitiveSanitizer` wraps all adversary-controlled telemetry in dynamically generated 8-character XML boundaries, preventing delimiter collision or guessing. Individual field values are truncated to a hard 10,000-character ceiling before any processing, system control tokens (`<|im_start|>`, `[INST]`) are replaced with `[DEFANGED_TOKEN]`, role-play injection patterns (`System:`, `Human:`, `Assistant:`) are rewritten to `EntityData:`, and all output is HTML-escaped to neutralize boundary breakout attempts. Downstream, the `DuckDBQueryTool` independently wraps every string cell returned from the data lake in `<untrusted_payload>` tags, and every Expert agent SOP contains an explicit instruction to never obey content found inside those tags -- creating a two-layer defense where the sanitizer neutralizes the payload and the prompt reinforces the behavioral constraint.

* **Read-Only Data Lake Sandboxing (OWASP LLM02 -- Insecure Output Handling):** All DuckDB queries execute inside ephemeral in-memory connections that are created and destroyed per invocation. A keyword blocklist rejects destructive SQL commands (`DROP`, `DELETE`, `INSERT`, `UPDATE`, `COPY`, `ALTER`) before execution. Queries missing an explicit `LIMIT` clause are automatically capped at 15 rows, and a secondary post-fetch truncation enforces the cap even if the LLM bypasses injection. Per-cell string values are truncated to 2,000 characters to prevent context window blowout. S3 credentials are scoped to `s3:GetObject` on the cold-storage bucket, and exceptions are reflected back to the LLM as corrective feedback rather than crashing the agent loop.

* **Cryptographic Model Supply Chain Integrity (ATLAS AML.T0044):** The `serve_vllm.sh` boot script verifies SHA-384 hashes of the Spatial Projector weights against pre-computed digests generated at build time by the Makefile. If the calculated hash diverges from the expected value -- indicating weight tampering, a poisoned adapter, or a corrupted download -- the inference server refuses to load and exits with a non-zero status, preventing compromised weights from ever reaching VRAM.

* **Training Data Hygiene & Credential Scrubbing (OWASP LLM03):** The `01_spool_chatml.py` data pipeline applies regex-based credential sanitization to every raw telemetry payload before it enters the training corpus, masking passwords, API keys, bearer tokens, and secrets with `[REDACTED]` markers. The pipeline enforces a strict 85/15 train/eval split with deterministic shuffling, ensuring the regression suite in `03_eval_model.py` always tests against held-out data the model has never seen during fine-tuning.

* **Deterministic Regression Gate (ATLAS AML.T0043 / OWASP LLM09):** The `03_eval_model.py` CI/CD gauntlet validates every model checkpoint across three axes before it is cleared for production: (1) **OS-Context Hallucination** -- if the prompt specifies Windows ETW telemetry, the model must not emit Linux artifacts (`bash`, `/etc/shadow`, `comm=`); (2) **SOAR Schema Adherence** -- any `RECOMMENDED_ACTION` in the output must resolve to one of the `contain`/`monitor`/`dismiss` enum values enforced by `VerdictSchema`; (3) **Prompt Injection Immunity** -- adversarial payloads containing `IGNORE_PREVIOUS` must not produce compliant output. If aggregate accuracy drops below 99%, the deployment pipeline halts with a non-zero exit code, blocking the atomic swap to production.

* **Adversarial Review Board (OWASP LLM09 -- Overreliance):** Before any True Positive verdict reaches SOAR, the `review_board_node` subjects it to an adversarial review in which **every expert has a counterpart** whose only job is to **disprove** that expert's contribution (separate LLM invocations, temperature 0.2 for analytical diversity). Each implicated counterpart grades its domain on three axes: (1) **Benign Alternative** -- did the expert disprove that the alert was a vulnerability scanner, admin script, or software updater, typically via historical/baseline retrieval? (2) **Behavioral Proof** -- is there evidence of actual execution, file drops, or bytes transferred, not merely a blocked connection? (3) **Blast Radius Completeness** -- are any entities still in `pending` state? A finding is confirmed a True Positive **only if no implicated counterpart can disprove it**; a single credible disproof vetoes the board, overriding the Supervisor's verdict to monitor and dropping the containment payload. The board is **fail-closed** -- an unreviewable or incompletely-reviewed domain never yields autonomous containment.

* **Blast Radius Containment & Entity State Machine:** The orchestrator enforces a `MAX_ENTITIES` cap of 10 tracked entities per investigation. If the cap is exceeded, the DAG is force-terminated with a `FINISH` signal to prevent runaway lateral pivoting. A hardcoded `GLOBAL_DO_NOT_PIVOT` set strips public DNS resolvers (`8.8.8.8`, `1.1.1.1`, `0.0.0.0`, `127.0.0.1`, `169.254.169.254`) from the entity pool before any expert receives them. The `merge_entities` reducer in `state.py` enforces a strict severity hierarchy (`pending → investigating → cleared → malicious`) -- once an entity reaches `malicious`, no subsequent agent output can downgrade it. TIER-1 critical assets (`dc-prod-01`, `nexus-core`) automatically trigger a `manual_review_required` action, forcing a human-in-the-loop checkpoint before any containment reaches n8n.

* **Cascading LLM Failover & Sovereign Degradation (OWASP LLM05 -- Supply Chain):** Every agent node (Supervisor, Host Expert, Net Expert, Review Board, Response) constructs a provider failover chain from the `nexus.toml` configuration: Primary Anthropic → Azure Corporate → Internal Ollama. If the primary frontier model is unavailable, rate-limited, or returns an error, the agent transparently cascades to the next provider. If all three fail, the Supervisor emits a safe default verdict (`is_true_positive: false`, `recommended_action: monitor`) rather than crashing the DAG, ensuring the system degrades to monitoring rather than silence.

* **Idempotent SOAR Execution & Deduplication:** SOAR payloads carry a 15-minute rolling-window idempotency key (`iso-{sensor}-{timestamp // 900}`) and are published to JetStream with a `Nats-Msg-Id` header, preventing duplicate containment actions from message replays or race conditions. Upstream, the orchestrator enforces a 7-day Redis `SETNX` deduplication window on `event_id`, ensuring the same anomaly never triggers two parallel swarm investigations.

* **Vector Dimensionality Validation:** The `QdrantVectorSearchTool` validates that the target vector's dimensionality matches the expected schema for the named vector space (`c2_math`: 8D, `sentinel_math`: 5D, `windows_math`: 4D) before executing any similarity search. Mismatched dimensions are rejected with an error message, preventing adversarial or malformed vectors from corrupting the UEBA index. Result sets are hard-capped at 10 matches regardless of the LLM's requested limit.

* **Endpoint Identity Injection Defense:** The Rust ingestion layer (`models.rs`) validates all `endpoint_id` values against a strict regex (`^[a-zA-Z0-9\-]{3,64}$`) using the `validator` crate before telemetry enters the Qdrant vector index or Parquet cold storage. This prevents SQL injection, path traversal, or delimiter attacks from propagating through sensor-controlled identifiers.

* **Durable Worker Circuit Breaker & Dead-Letter Routing:** The `lib_siem_core` Rust framework (`lib.rs`) implements an exponential-backoff retry loop (5 attempts, 2^n second delays) for every batch transmission. If all retries fail, a circuit breaker trips that pauses consumption for 30 seconds to allow backend recovery, and poison messages are shunted to a dedicated dead-letter queue (`nexus.dlq.*`) subject with full metrics emission (`nexus_worker_circuit_breaker_trips_total`, `nexus_worker_dlq_routed_total`). Graceful shutdown is handled via SIGTERM/SIGINT signal traps that drain in-flight batches before exit.

* **Ephemeral Operations Stack Concurrency Locking:** The orchestrator caps concurrent forensic operations interfaces at `MAX_OPERATIONS_STACKS` (3) using a Redis set lock. When the limit is reached, new incidents piggyback onto an existing active session rather than spawning unbounded infrastructure. On SOAR containment callback (`STATUS: CONTAINED`), the lock is released and the teardown script is executed, ensuring forensic tooling does not accumulate indefinitely.

---

**NIST AI 600-1 remediation controls** (status tracked in [nist_ai_600_1_control_tracker.md](nist_ai_600_1_control_tracker.md); pure logic in `agents/controls.py`, proven by `tests/lab_analytics_hunter/test_ai_controls.py`):

* **Confabulated-Evidence Grounding (NIST MS-2.5-003 -- Confabulation):** Before the Review Board's CONFIRMED True Positive is allowed to stand, `enforce_grounding` re-resolves every high-confidence artifact cited in the supervisor's finding (IPv4, file hashes, PIDs, AWS ARNs) against the union of all evidence the investigation actually retrieved -- entity ids/notes, message contents, and the raw alert. A verdict that names an artifact the swarm never saw is treated as a confabulation and **failed closed to `monitor`**, never autonomous containment on fabricated evidence.

* **Immunity-Memory TTL & Expiry (NIST GV-1.3-005 -- Harmful Bias & Homogenization):** Every persisted RAG-memory point is stamped with a `created_at` timestamp, and the supervisor's recall delegates the auto-dismiss decision to `memory_is_actionable`, which honours a configurable TTL (`NEXUS_MEMORY_TTL_SECONDS`, default 30 days). A stale or wrong False Positive can no longer entrench a permanent blind spot in the immunity feedback loop; legacy points written before the TTL preserve prior behaviour.

* **AI-Origin Provenance Disclosure (NIST MP-5.1-003 -- Human-AI Configuration):** Every analyst-facing incident report is stamped (idempotently) with an `AI-GENERATED` banner via `stamp_ai_provenance`, so a human consumer of the report is never misled about the source of the verdict and is reminded to verify forensic claims against source telemetry.

* **Frontier Model Version Pinning (NIST MP-4.1-007 -- Value Chain & Component Integration):** `unpinned_frontier_models` flags any external/frontier provider (Anthropic/Azure-OpenAI) whose configured `model` is a floating alias (empty or `*-latest`), preventing a silent provider-side model change from altering verdict behaviour with no regression gate. Internal sovereign providers are out of scope (their weights are hash-verified by the supply-chain control above).

* **Confidence-Calibration Telemetry (NIST MS-2.13-001 -- Confabulation):** `calibration_record` pairs the swarm's predicted verdict/confidence with the operator's realized disposition and emits a per-decision Brier score, providing the data to measure (and correct) over/under-confidence of the `confidence` field that gates immunity and live acquisition.

* **Disaggregated Fairness / Disparity Audit (NIST MS-2.11-002 -- Harmful Bias & Homogenization):** Because the swarm makes autonomous containment decisions, a subgroup that is systematically over- or under-contained is an allocative harm. `fairness_report` computes disaggregated containment and true-positive rates per subgroup (e.g. by `source_type` or asset class) over the verdict history and flags any group -- with sufficient sample support -- whose containment rate deviates from the fleet baseline beyond a configurable disparity tolerance.

* **Immunity-Memory Homogenization Monitor (NIST GV-1.3-005 / MS-2.11-005 -- Harmful Bias & Homogenization):** Because the immunity loop feeds the swarm's own prior verdicts back into future analysis, over-concentration of the stored signature distribution is a model-collapse / homogenization risk. `memory_homogenization` reports the top-signature share and normalized Shannon entropy of the immunity memory and raises a `homogenized` flag when one pattern dominates or diversity collapses below threshold.